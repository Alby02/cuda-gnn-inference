At least 2 GNN Types are required to be implemented in the project.
A comparison in the performance with a different framework is also required.
At least 1 not exactly one but at least one implementation for for multi-threaded CPU inference is required.
At least 1 CUDA inference implementation is required; more than one is allowed.
Only 1 of the 3 suggested synthetic graph families is required; the others are optional.
The shared-memory comparison is required only when shared memory is applicable to the selected CUDA implementation.
Feature compression is not included in the selected project requirements (dense matrix can't be compressed using CSC CSR formats in a meaningful way).